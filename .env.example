BASE_URL=https://test.os-hub.net
USER_EMAIL={email}
USER_PASSWORD={password}
AUTH_TOKEN={token}
USER_ADMIN_EMAIL={admin_email}
USER_ADMIN_PASSWORD={admin_password}
USER_API_EMAIL={api_email}
USER_API_PASSWORD={api_password}

ENVIRONMENT={environment}

VERSION_TAG={version_tag}

# QAlity Plus — used by scripts/qality_mark_automated.py (post-regression)
QALITY_API_TOKEN={qality_api_token}

# Optional Jira basic auth to map issue ids → OSDEV-####
# JIRA_EMAIL={email}
# JIRA_API_TOKEN={jira_api_token}
# JIRA_BASE_URL=https://opensupplyhub.atlassian.net